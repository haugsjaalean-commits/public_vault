

Paste image rename allows you to automatically rename images when they are pasted into your vault. I think that this is pretty useful, because otherwise it can become pretty hard to find images when they are all being lumped into the same "attachments" folder. 

![[Paste image rename.png]]