When child GPs are created, step 3 is skipped.

This means that parent GPs still contain all of the KIs of their children. It also means that a given KI can exist in multiple branches of the KG at once.
